interface:
  display_name: "Launch Fastlane (compatibility)"
  short_description: "Delegate setup and resume to Fastlane"
  default_prompt: "Use $fastlane to handle this init template or resume request through the single Fastlane coordinator."
policy:
  allow_implicit_invocation: false
