interface:
  display_name: "Maintain Fastlane"
  short_description: "Safely maintain the Fastlane framework"
  default_prompt: "Use $maintain-fastlane to make the scoped framework change without starting an application lifecycle."
policy:
  allow_implicit_invocation: true
